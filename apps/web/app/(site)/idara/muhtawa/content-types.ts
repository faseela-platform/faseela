/**
 * Content-type vocabulary, re-exported from the shared component module so the
 * authoring forms and the public Feed draw from one source (the labels are shown on
 * both). See `../../components/content-types`.
 */
export { CONTENT_TYPE_LABEL, CONTENT_TYPES, type ContentType } from "../../components/content-types";
